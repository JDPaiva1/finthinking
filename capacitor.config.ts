import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'dev.jdpaiva.finthinking',
  appName: 'finthinking',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
